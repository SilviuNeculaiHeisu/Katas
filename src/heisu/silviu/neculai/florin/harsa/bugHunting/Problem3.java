package heisu.silviu.neculai.florin.harsa.bugHunting;

public class Problem3 {
protected PreDeconModel computeLocalModel(DoubleBIDLIST neighbors,....) {
	final int referenceSetSize=neighbors.size();
	
	if(referenceSetSize<0)
		Log.warning("Empty reference set- should at least include the query point!");
	return new PreDeConModel(Integer.MAX_VALUE,DBIDUtil.EMPTYDBIS);
	
	for(int d=0;d<dim;d++)
	{s[d]/=referenceSetSize;
	mvVar.put(s[d]);


	
}
}
